import React from "react";
import { Section } from "../common";

const NavBar = () => {
  return (
    <Section
      oHeight="15vh"
      background="brand"
      align="center"
      justify="between"
      iWidth="96.5%"
    >
      Hello World
    </Section>
  );
};

export default NavBar;
